package com.minimals.client.sound;

/**
 * Marker for widgets that must not play the hover sound even though they extend a vanilla
 * button (pure labels, decorative rows). Implement it on the widget class; nothing to override.
 */
public interface SilentHover {
}
